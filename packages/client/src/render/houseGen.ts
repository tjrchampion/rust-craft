/** Re-export shared house generator so the editor keeps a stable import path. */
export {
  generateHouseAssets,
  expandHouseToAssets,
  expandHousesToAssets,
  resolveHouseType,
  regionAllAssets,
  HOUSE_TYPE_OPTIONS,
  type HouseType,
  type HouseOptions,
  type RegionHouse,
} from "@rustcraft/shared";
