node_modules/
dist/
.output/
.nitro/
.env
*.local
.DS_Store

# Raw purchased/downloaded CraftPix source packs (PSDs + full icon sets, ~800MB).
# Only the curated derivatives we actually ship live in packages/client/public/assets/ui/.
packages/ui/
